# Production Environment Variables Template
# Copy this file to .env.production and fill in the actual values

# ================================
# REQUIRED PRODUCTION VARIABLES
# ================================

# Database Configuration
DATABASE_URL="mysql://username:password@host:port/database_name"
# Example: DATABASE_URL="mysql://dsms_user:secure_password@db.example.com:3306/dsms_production"

# JWT Secret (CRITICAL: Must be 32+ characters, cryptographically secure)
JWT_SECRET="your-super-secure-jwt-secret-at-least-32-characters-long"
# Generate with: openssl rand -base64 32

# Application Environment
NODE_ENV="production"

# ================================
# OPTIONAL PRODUCTION VARIABLES
# ================================

# Application URL (for redirects, emails, etc.)
NEXT_PUBLIC_APP_URL="https://your-domain.com"

# Server Port (default: 3000)
PORT="3000"

# Database Connection Details (if not using DATABASE_URL)
DB_HOST="your-database-host"
DB_PORT="3306"
DB_USER="your-database-user"
DB_PASSWORD="your-database-password"
DB_NAME="your-database-name"

# ================================
# DEPLOYMENT-SPECIFIC VARIABLES
# ================================

# For Vercel deployment
VERCEL_URL="your-app.vercel.app"

# For Railway deployment
RAILWAY_STATIC_URL="your-app.railway.app"

# For custom domain
CUSTOM_DOMAIN="your-custom-domain.com"

# ================================
# SECURITY CONSIDERATIONS
# ================================

# 1. JWT_SECRET: Use a cryptographically secure random string
#    Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# 2. DATABASE_URL: Use strong database credentials
#    - Complex password with special characters
#    - Dedicated database user with minimal permissions
#
# 3. Environment Security:
#    - Never commit this file with real values
#    - Use your deployment platform's environment variable system
#    - Rotate secrets regularly

# ================================
# DEPLOYMENT CHECKLIST
# ================================

# Before deploying to production:
# [ ] Set strong JWT_SECRET (32+ characters)
# [ ] Configure production database
# [ ] Set NEXT_PUBLIC_APP_URL to your domain
# [ ] Test database connection
# [ ] Run database migrations
# [ ] Seed initial data if needed
# [ ] Configure SSL/HTTPS
# [ ] Set up monitoring and logging
# [ ] Configure backup strategy
